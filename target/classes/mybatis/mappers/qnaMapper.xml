<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="qnaMapper">
  	
  	<select id="boardList" parameterType="com.coo.s4.util.Pager" resultType="com.coo.s4.model.BoardQnAVO">
  		
  		select * from (select q.*, ROWNUM R from    (select * from qna 
  		<choose>
  			<when test="kind == 'kT' || kind == '' ">where title like '%'||#{search}||'%'</when>
  			<when test="kind == 'kW'">where writer like '%'||#{search}||'%'</when>
  			<when test="kind == 'kC'">where contents like '%'||#{search}||'%'</when>
  		</choose>
  		ORDER BY num DESC) Q )
		WHERE r BETWEEN #{startRow} and #{lastRow}
  	
  	</select>
  
  	<select id="boardCount" resultType="java.lang.Integer" parameterType="com.coo.s4.util.Pager">
  		select count(num) from (select Q.*, ROWNUM R from    (select * from qna 
  		<choose>
  			<when test="kind == 'kT' || kind == '' ">where title like '%'||#{search}||'%'</when>
  			<when test="kind == 'kW'">where writer like '%'||#{search}||'%'</when>
  			<when test="kind == 'kC'">where contents like '%'||#{search}||'%'</when>
  		</choose>
  		ORDER BY num DESC) Q )
  	</select>
  
  
  
  
  
  </mapper>